name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@e7797886157a51b1eb44d9ef04455990b4d26452 # 2.0.58
    with:
      image-name: cloudnative-pg-postgres-18
      create-artifacts: false
